# CI/CD

1. [Vercal deployment](/.github/workflows/vercel-deploy.yml) `--archive=tgz option`: deploy as bundle instead of separate files to avoid hitting Vercel free tier limits. May take slightly longer to set up but is worth it for us.
